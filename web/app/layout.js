export const metadata = {
  title: "ICT NSE Chart",
  description: "ICT / SMC chart and bar replay"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
